#include "holberton.h"
/**
 * print_line - check the code for Holberton School students.
 * @n : check
 * Return: Always 0.
 */
void print_line(int n)
{
		for (n = 0 ; n < n ; n++)
		{
			if (n > 0)
				_putchar ('_');
		}
		_putchar (10);
}
